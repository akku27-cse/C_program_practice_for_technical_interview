#include<stdio.h>
#include<math.h>
void main()
{
	int a,b,c;
	float s,area;
	area=sqrt(s*(s-a)*(s-b)*(s-c));
	printf("\n enter the value  of A,B,C:");
	scanf("%d%d%d",&a,&b,&c);
	s=(a+b+c)/2
	area=sqrt(s*(s-a)*(s-b)*(s-c));
	printf("\n area of a tringle=%f",area);
}
